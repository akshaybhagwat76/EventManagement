@model IEnumerable<MiidWeb.TicketClassSeatRange>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RowNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FromSeatNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ToSeatNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TicketClass.Code)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RowNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FromSeatNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ToSeatNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TicketClass.Code)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
                @Html.ActionLink("Details", "Details", new { id=item.ID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.ID })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
